# 项目管理线上版开发 TO-DO

> **执行要求：** 本文件是跨子系统的主路线图。执行具体里程碑前，必须使用 `superpowers:writing-plans` 为该里程碑生成独立实施计划；实施时使用 `superpowers:subagent-driven-development` 或 `superpowers:executing-plans`，逐任务测试、审查和提交。

**目标：** 建设可由浏览器、macOS 和 Windows 使用的项目管理线上系统，桌面端支持离线编辑与恢复网络后自动同步。

**架构：** React/TypeScript 客户端共用领域规则，浏览器通过 HTTPS API 在线工作；Tauri 桌面端先写本机 SQLite，再通过同步引擎与模块化单体 API 交换增量数据。中央业务数据保存到 PostgreSQL，照片和附件保存到国内云对象存储。

**首期容量：** 200 名用户、1000 个项目、文件每年增长约 200GB。

---

## 0. 现在应该先做什么

不要先复制全部旧界面，也不要先购买大量云资源。第一步是完成以下三份设计基线，并由业务负责人确认：

- [ ] 权限矩阵：谁能看到什么、谁能修改什么。
- [ ] 领域模型：项目、合同、回款等记录如何拆分，哪些字段是权威数据。
- [ ] 同步协议：离线操作如何编号、上传、覆盖、删除和重试。

这三项决定数据库、API、离线存储和数据迁移。未确认前开始全量编码，会造成重复返工。

## 1. 全局约束

- [ ] 现有“项目管理软件”保持独立，不在原仓库直接改造成线上版。
- [ ] 普通用户只能查看和编辑获授权项目。
- [ ] 领导可以查看全部项目资料，编辑权限仍按项目授权控制。
- [ ] 管理员负责账号、角色、项目授权和系统配置，并可全局新增、修改和逻辑删除项目业务数据。
- [ ] 账号由管理员开通，不提供公开注册。
- [ ] 浏览器首期在线使用。
- [ ] macOS 和 Windows 桌面端支持离线编辑与自动同步。
- [ ] macOS 保留日历和提醒事项能力。
- [ ] Windows 首期不接入 Windows 日历或 Microsoft To Do。
- [ ] 并发修改按服务器最后有效提交覆盖，粒度为单条业务记录。
- [ ] 文件保存到国内云对象存储，桌面端保留本地缓存和待上传队列。
- [ ] 不在源码、日志、文档或测试数据中写入密码、密钥、令牌和真实个人敏感信息。
- [ ] 首期采用模块化单体，不建设微服务和 Kubernetes 集群。
- [ ] 开发、测试和生产环境完全隔离。

## 2. 通用完成标准

每个开发任务必须同时满足：

- [ ] 先编写能够失败的自动化测试，再实现最小功能。
- [ ] 权限判断在服务端执行，不能只依赖前端展示控制。
- [ ] 新增接口有输入校验、错误码和权限测试。
- [ ] 新增数据变更有迁移、回退和兼容说明。
- [ ] 新增离线操作通过断网、重试、重复提交和覆盖测试。
- [ ] 新增文件操作通过权限、失败重试和校验值测试。
- [ ] 日志不包含敏感信息和完整文件内容。
- [ ] `npm run lint` 通过。
- [ ] `npm run typecheck` 通过。
- [ ] `npm run test -- --run` 通过。
- [ ] 涉及桌面 Rust 代码时 `cargo test --locked` 通过。
- [ ] Web、macOS 或 Windows 受影响端完成构建验证。
- [ ] 文档与实际接口、命令和目录结构一致。
- [ ] 每个可独立验收任务单独提交，不混入无关重构。

---

## M0：设计基线与决策冻结

**优先级：** P0，所有编码任务的前置条件。

**计划创建：**

- `docs/architecture/permission-matrix.md`
- `docs/architecture/domain-model.md`
- `docs/architecture/sync-protocol.md`
- `docs/architecture/file-storage.md`
- `docs/architecture/deployment-topology.md`
- `docs/decisions/0001-modular-monolith.md`
- `docs/decisions/0002-record-level-last-write-wins.md`

### M0.1 权限矩阵

- [ ] 列出普通用户、领导、管理员三类基础角色。
- [ ] 定义项目成员、项目负责人和临时授权关系。
- [ ] 对项目、合同、回款、进度、签证、造价、风险、待办、照片逐项定义查看、新增、编辑、删除、导出权限。
- [ ] 明确领导默认全局只读，需项目授权才能编辑。
- [ ] 明确管理员无需项目成员关系即可全局新增、修改和逻辑删除业务数据。
- [ ] 定义用户停用、项目权限取消后的本地缓存和待同步操作处理规则。
- [ ] 定义导出、下载和敏感字段查看权限。
- [ ] 用至少 12 个“角色 × 操作 × 项目归属”示例验证无歧义。

**验收：** 任意一个 API 操作都能在权限矩阵中找到唯一结论。

### M0.2 领域模型

- [ ] 逐项梳理现有本地版 `Project` 聚合中的业务对象。
- [ ] 将项目、项目成员、合同、合同节点、回款、里程碑、签证、造价版本、风险、待办、照片、附件拆为独立记录。
- [ ] 为每类记录定义唯一标识、所属项目、创建人、修改人、服务器版本、删除标记和审计字段。
- [ ] 明确合同总额、累计回款、应回款、毛利率等派生字段的权威计算位置。
- [ ] 明确金额精度、日期时区、状态枚举和空值规则。
- [ ] 明确旧版字段到新版记录的迁移映射。
- [ ] 使用完全虚构的测试项目核对所有关联关系。

**验收：** 每一项现有业务数据都有唯一归属和迁移目标，不存在同时由多个字段维护的金额口径。

### M0.3 同步协议

- [ ] 定义同步操作信封：`operationId`、`deviceId`、`entityType`、`entityId`、`action`、`payload`、`clientSequence`。
- [ ] 定义服务器结果：接受、重复、无权限、校验失败、临时失败。
- [ ] 定义服务器提交序号和客户端拉取游标。
- [ ] 定义记录级最后提交覆盖规则。
- [ ] 定义新增、新增后离线修改、删除、删除后离线修改的处理顺序。
- [ ] 定义重复提交的幂等规则。
- [ ] 定义用户权限被取消时待上传操作的拒绝和本地提示。
- [ ] 定义批次大小、重试退避、失败隔离和手动重试规则。
- [ ] 定义同步状态：未同步、同步中、已同步、失败、权限已失效。
- [ ] 绘制首次同步、正常增量同步、离线恢复和冲突覆盖时序图。

**验收：** 使用两台设备模拟相同记录的离线编辑，可以明确推导最终值和审计记录。

### M0.4 文件与部署设计

- [ ] 定义对象键、文件元数据、校验值和项目关联方式。
- [ ] 定义分片上传、断点续传、重复文件和失败清理规则。
- [ ] 定义本地缓存目录、缓存上限和清理策略。
- [ ] 定义文件删除恢复期和对象存储生命周期。
- [ ] 定义开发、测试、生产三套环境拓扑。
- [ ] 定义数据库备份、对象存储保护和恢复演练频率。
- [ ] 确认国内云厂商、域名主体和 ICP 备案责任人。

**里程碑出口：** 上述文档完成业务与技术双重评审后，才进入 M1。

---

## M1：工程基线与最小运行链路

**目标：** 建立可持续开发的独立仓库，并跑通 Web 到 API 的最小链路。

**计划创建：**

- `package.json`
- `package-lock.json`
- `tsconfig.base.json`
- `.gitignore`
- `.env.example`
- `apps/web/`
- `apps/api/`
- `apps/desktop/`
- `packages/domain/`
- `packages/ui/`
- `packages/sync/`
- `.github/workflows/verify.yml` 或对应代码托管平台流水线

### M1.1 仓库与工具链

- [x] 使用 npm workspaces 建立单仓库结构。
- [x] 统一 TypeScript 严格模式、路径别名和构建目标。
- [x] 配置 lint、类型检查、单元测试和格式检查命令。
- [x] 创建不含真实值的 `.env.example`。
- [x] 将 `.env`、证书、安装包、数据库文件、本地缓存和构建产物加入 `.gitignore`。
- [x] 建立提交前和 CI 验证命令。
- [x] 在 README 中补充真实可执行的开发命令。

### M1.2 最小应用链路

- [x] Web 显示明确的开发环境页面和 API 连通状态。
- [x] API 提供版本化健康检查接口。
- [x] 共享领域包导出一个版本常量并同时被 Web、API 引用。
- [x] 为健康检查、配置缺失和共享包引用编写测试。
- [x] 在 CI 中执行 lint、typecheck、test 和 build。

**验收命令：**

```bash
npm ci
npm run lint
npm run typecheck
npm run test -- --run
npm run build
```

**里程碑出口：** 新机器仅依据 README 即可启动 Web 和 API，CI 全部通过。

---

## M2：账号、登录与权限闭环

**目标：** 管理员可以开通账号，用户登录后只能访问授权项目。

**计划创建：**

- `apps/api/src/modules/auth/`
- `apps/api/src/modules/users/`
- `apps/api/src/modules/authorization/`
- `apps/web/src/features/auth/`
- `apps/web/src/features/admin-users/`
- `packages/domain/src/auth/`

### M2.1 服务端身份能力

- [x] 建立用户、角色、设备会话和密码重置模型。
- [x] 实现管理员创建、启用、停用账号。
- [x] 实现登录、退出、刷新会话和强制下线。
- [x] 密码使用安全哈希，禁止明文落盘和写入日志。
- [x] 登录失败返回统一错误，避免泄露账号是否存在。
- [x] 增加连续失败限制和安全审计记录。
- [x] 为停用用户、过期会话、重复刷新和越权请求编写测试。

### M2.2 权限能力

- [x] 将 M0 权限矩阵实现为统一授权服务。
- [ ] 服务端从登录身份计算项目数据范围。
- [ ] 普通用户仅获得已授权项目。
- [ ] 领导获得全部项目只读范围。
- [ ] 管理员获得系统管理范围和全部项目业务读写范围。
- [ ] 对读取、修改、删除、导出和文件下载分别测试权限。
- [x] 验证管理员角色撤销后，未加入项目的待同步写操作被拒绝。

### M2.3 Web 登录与管理

- [x] 完成登录、会话失效和退出界面。
- [x] 完成管理员用户列表、开通、停用和重置入口。
- [x] 禁止在浏览器存储中保存长期明文凭证。
- [x] 对登录、无权限、会话过期和管理员操作编写交互测试。

**里程碑出口：** 三类角色通过自动化权限矩阵测试，任何未授权项目请求均被服务端拒绝。

---

## M3：项目核心在线纵向切片

**目标：** 首先交付“登录 → 项目列表 → 项目详情 → 编辑 → 审计”的完整在线闭环，而不是同时开发所有模块。

**计划创建：**

- `apps/api/src/modules/projects/`
- `apps/api/src/modules/audit/`
- `apps/web/src/features/projects/`
- `packages/domain/src/projects/`

- [x] 实现项目基础信息和项目成员记录。
- [x] 实现普通用户项目范围、领导全局查看和管理员全局业务编辑。
- [x] 实现项目列表、筛选、详情、新增和编辑 API。
- [x] 每次有效修改生成服务器提交序号和审计记录。
- [x] 实现浏览器项目列表、详情和编辑界面。
- [x] 实现保存中、成功、校验失败、无权限和网络失败状态。
- [ ] 使用虚构数据完成项目纵向集成测试。
- [ ] 验证两个用户同时修改项目基础记录时后提交者生效。
- [x] M3 代码完成：虚构种子、项目纵向切片与非数据库自动化验证已就绪。
- [ ] M3 运行验收待完成：在隔离 PostgreSQL 执行 `test:db`，并完成虚构角色的桌面与窄屏浏览器流程。

**里程碑出口：** 浏览器端核心项目流程可供小范围内部试用，权限和审计可追溯。

---

## M4：macOS 与 Windows 桌面基础

**目标：** 建立共享 UI、平台适配和本机 SQLite 数据层，暂不接完整同步。

**计划创建：**

- `apps/desktop/src-tauri/`
- `apps/desktop/src/platform/`
- `apps/desktop/src/local-db/`
- `apps/desktop/src/repository/`
- `apps/desktop/src/features/sync-status/`

- [ ] 初始化 Tauri 桌面工程并复用共享 UI。
- [ ] 分离 macOS、Windows 和 Web 平台能力接口。
- [ ] 建立 SQLite 迁移和版本记录机制。
- [ ] 定义 `ProjectRepository`，分别实现在线和本地适配器。
- [ ] 本地业务写入与待同步操作在同一事务完成。
- [ ] macOS 凭证保存到 Keychain。
- [ ] Windows 凭证保存到 Credential Manager。
- [ ] 完成无网络启动、离线项目查看和离线项目编辑。
- [ ] 建立 macOS 与 Windows 构建验证。
- [ ] 确认 Windows 包不包含待办系统集成代码和权限。

**里程碑出口：** 两个平台在断网环境下能够打开并编辑本机测试项目，重启后数据不丢失。

---

## M5：业务数据同步引擎

**目标：** 完成可重试、幂等、可审计的双向增量同步。

**计划创建：**

- `packages/sync/src/protocol/`
- `apps/desktop/src/sync/`
- `apps/api/src/modules/sync/`
- `apps/api/src/modules/devices/`

### M5.1 推送与幂等

- [ ] 按 M0 协议生成全局唯一操作。
- [ ] 实现批量推送接口和单操作结果。
- [ ] 服务器记录已处理操作，重复推送返回原结果。
- [ ] 无权限操作进入明确失败状态，不无限重试。
- [ ] 临时错误按退避规则重试。
- [ ] 验证请求超时后重复提交不会创建重复记录。

### M5.2 拉取与游标

- [ ] 服务器为每次有效修改分配递增提交序号。
- [ ] 客户端按游标拉取有权限的数据变化。
- [ ] 同一批变化和游标在本地事务中提交。
- [ ] 游标失效时支持受控全量重建。
- [ ] 权限取消后停止返回项目数据，并产生权限变化指令。

### M5.3 覆盖、删除与状态

- [ ] 实现记录级服务器最后提交覆盖。
- [ ] 实现删除墓碑及跨设备传播。
- [ ] 实现同步中、已同步、失败和权限失效界面。
- [ ] 为用户提供失败原因和手动重试入口。
- [ ] 审计日志记录覆盖关系，不记录敏感完整载荷。

### M5.4 故障测试

- [ ] 上传前断网。
- [ ] 服务端提交成功但客户端未收到响应。
- [ ] 上传一半后应用退出。
- [ ] 两台设备离线修改同一记录。
- [ ] 一台设备删除、另一台设备离线修改。
- [ ] 用户离线编辑后被取消项目权限。
- [ ] 本地数据库迁移失败并安全回滚。

**里程碑出口：** 自动化故障矩阵全部通过，连续离线 7 天后恢复网络能够得到确定且可追溯的最终状态。

---

## M6：照片与附件同步

**目标：** 文件在浏览器和两种桌面端安全可用，并支持离线缓存和断点续传。

**计划创建：**

- `apps/api/src/modules/files/`
- `apps/desktop/src/file-cache/`
- `apps/desktop/src/file-transfer/`
- `apps/web/src/features/files/`
- `packages/domain/src/files/`

- [ ] 建立文件元数据、上传会话和项目关联模型。
- [ ] 生成与账号、项目、环境隔离的对象键。
- [ ] 实现分片上传、续传、完成确认和校验值验证。
- [ ] 上传完成前业务记录显示“待上传”，不能伪装为已同步。
- [ ] 桌面端保存本地缓存索引和空间占用。
- [ ] 建立缓存上限、最近使用保留和手动清理。
- [ ] 浏览器使用短时授权地址预览与下载。
- [ ] 项目权限取消后停止续传和下载。
- [ ] 文件删除进入恢复期，超过期限后再物理清理。
- [ ] 测试断网续传、重复文件、损坏分片、无权限下载和对象不存在。

**里程碑出口：** 大文件断点续传与权限测试通过，客户端重启不丢失上传队列。

---

## M7：分批迁移业务模块

每一批都必须完成领域规则、API、Web、桌面本地表、同步、权限、审计和迁移测试后才能进入下一批。

### M7.1 第一批：经营主链路

- [ ] 项目成员与负责人
- [ ] 合同与合同付款节点
- [ ] 回款、累计回款、结算和应回款
- [ ] 金额口径与现有本地版对照测试

### M7.2 第二批：执行与成本

- [ ] 进度与里程碑
- [ ] 签证管理
- [ ] 造价 Excel 导入
- [ ] 造价版本、利润指标和预警

### M7.3 第三批：督办闭环

- [ ] 风险管理
- [ ] 待办管理
- [ ] macOS 日历和提醒事项同步适配
- [ ] Windows 明确显示不提供系统待办同步，不出现无效入口

### M7.4 第四批：资料与汇总

- [ ] 项目照片关联
- [ ] 项目归档与恢复
- [ ] 项目总览和经营指标
- [ ] 数据导入、完整备份和脱敏分享导出

**里程碑出口：** README 中列出的首期业务模块均有端到端测试，关键计算与本地版一致。

---

## M8：国内云部署与运维基线

**计划创建：**

- `infra/containers/`
- `infra/environments/`
- `docs/operations/deployment.md`
- `docs/operations/backup-restore.md`
- `docs/operations/incident-response.md`

- [ ] 完成企业云账号、域名、ICP 备案和证书准备。
- [ ] 建立开发、测试、生产三套隔离环境。
- [ ] 应用服务使用最小权限访问数据库和对象存储。
- [ ] 数据库和对象存储不直接开放公网访问。
- [ ] 真实密钥进入云密钥管理服务，不进入代码仓库。
- [ ] 建立结构化日志、运行指标、同步失败和容量告警。
- [ ] 配置数据库自动备份、保留周期和恢复演练。
- [ ] 配置对象存储加密、版本保护、生命周期和备份。
- [ ] 建立无敏感信息的部署模板和回滚命令。
- [ ] 完成一次测试环境故障与恢复演练。

**里程碑出口：** 测试环境可重复部署和回滚，备份能够在隔离环境恢复。

---

## M9：跨平台发布与自动更新

- [ ] 申请并保护 macOS 代码签名与公证所需凭证。
- [ ] 申请并保护 Windows 代码签名凭证。
- [ ] 分别建立 macOS 和 Windows 构建流水线。
- [ ] 生成签名安装包和版本清单。
- [ ] 建立受签名保护的 Tauri 自动更新流程。
- [ ] 更新失败时保留当前可运行版本。
- [ ] 验证从前一正式版本升级到当前版本。
- [ ] 验证更新前存在未同步操作时不会丢失本地数据。
- [ ] 建立紧急停止发布和回退流程。

**里程碑出口：** macOS 和 Windows 均可安装、升级、回退，未同步数据在升级前后保持完整。

---

## M10：旧版迁移、试运行与正式上线

### M10.1 迁移工具

- [ ] 定义旧版导出包格式和版本号。
- [ ] 校验项目、合同、回款、照片路径和造价版本结构。
- [ ] 生成迁移预览、警告清单和汇总对账结果。
- [ ] 迁移操作支持重复执行而不重复导入。
- [ ] 迁移失败不修改原始本地数据。
- [ ] 迁移日志自动屏蔽个人敏感信息。

### M10.2 对账

- [ ] 项目数量一致。
- [ ] 合同总额一致。
- [ ] 结算金额一致。
- [ ] 累计回款和应回款一致。
- [ ] 项目成员、风险、待办和里程碑数量一致。
- [ ] 照片和附件缺失项形成可处理清单。
- [ ] 造价关键指标和版本数量一致。

### M10.3 试运行

- [ ] 选择 10 至 20 名用户和代表性项目试运行。
- [ ] 同时覆盖浏览器、macOS 和 Windows。
- [ ] 模拟办公网络中断和恢复。
- [ ] 收集同步失败、权限误判和文件问题。
- [ ] 修复阻断问题后重新执行完整验收。

### M10.4 上线

- [ ] 确认旧系统停止写入时间。
- [ ] 执行最终导出、迁移和对账。
- [ ] 开放线上系统生产访问。
- [ ] 旧系统转为只读并保留受控备份。
- [ ] 准备上线值守、故障联系人和回退判断标准。

**里程碑出口：** 业务负责人签署对账与上线确认，旧版只读备份可恢复。

---

## M11：容量、安全与最终验收

- [ ] 使用脱敏或合成数据构造 200 名用户和 1000 个项目。
- [ ] 按三年文件规模验证对象存储策略，不实际复制敏感生产文件。
- [ ] 压测登录、项目列表、项目详情、增量同步和文件授权接口。
- [ ] 验证慢网、断网、重复请求和客户端时钟错误。
- [ ] 验证普通用户无法枚举、导出或下载未授权项目。
- [ ] 验证领导全局只读范围和管理员全局业务读写范围。
- [ ] 执行依赖、桌面权限、API 权限和敏感日志检查。
- [ ] 完成数据库恢复和对象恢复演练。
- [ ] 完成 macOS、Windows 和主流浏览器兼容性验收。
- [ ] 汇总遗留问题，阻断项归零后发布首个正式版本。

## 3. 推荐执行顺序

```text
M0 设计基线
  ↓
M1 工程基线
  ↓
M2 账号权限
  ↓
M3 项目在线纵向切片
  ↓
M4 桌面本地数据
  ↓
M5 同步引擎
  ↓
M6 文件同步
  ↓
M7 业务模块分批迁移
  ↓
M8 云部署 ── M9 跨平台发布
  ↓
M10 数据迁移和试运行
  ↓
M11 最终验收
```

## 4. 下一项可执行任务

下一步只执行 **M0：设计基线与决策冻结**，不要同时开始 M1。具体顺序：

1. 编写权限矩阵。
2. 编写领域模型和旧版迁移映射。
3. 编写同步协议与时序图。
4. 编写文件存储和部署拓扑。
5. 组织业务与技术评审并修订。

M0 通过后，再为 M1 单独生成逐文件、逐测试、逐提交的实施计划。
