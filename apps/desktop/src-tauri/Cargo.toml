[package]
name = "project-online-desktop"
version = "0.1.0"
edition = "2021"

[lib]
name = "project_online_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2" }

[dependencies]
tauri = { version = "2" }

[dev-dependencies]
serde_json = "1"
